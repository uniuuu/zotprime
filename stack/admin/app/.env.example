# APP_NAME=Laravel  # Moved to config.yaml
# APP_ENV=local  # Moved to config.yaml
# APP_KEY is now set via environment variable from root .env
# APP_KEY=
# APP_DEBUG=true  # Moved to config.yaml
# APP_URL is now set via environment variable from root .env
# APP_URL=http://localhost

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

# LOG_CHANNEL=stack  # Moved to config.yaml
# LOG_STACK=single  # Moved to config.yaml
# LOG_DEPRECATIONS_CHANNEL=null  # Moved to config.yaml
# LOG_LEVEL=debug  # Moved to config.yaml

# DB_CONNECTION=sqlite  # Moved to config.yaml
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

# SESSION_DRIVER=database  # Moved to config.yaml
# SESSION_LIFETIME=120  # Moved to config.yaml
# SESSION_ENCRYPT=false  # Moved to config.yaml
# SESSION_PATH=/  # Moved to config.yaml
# SESSION_DOMAIN=null  # Moved to config.yaml

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

# CACHE_STORE=database  # Moved to config.yaml
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

# REDIS_CLIENT=phpredis  # Moved to config.yaml
# REDIS_HOST=127.0.0.1  # Moved to config.yaml
# REDIS_PASSWORD=null  # Moved to config.yaml
# REDIS_PORT=6379  # Moved to config.yaml

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"
