apiVersion: v1
kind: Secret
metadata:
  name: {{ include "zotprime-k8s.fullname" . }}-webadmin-secret
  labels:
  {{- include "zotprime-k8s.labels" . | nindent 4 }}
data:
  webadmin-password: {{ required "webAdminConfig.password is required" .Values.webAdminConfig.password }}
type: Opaque
