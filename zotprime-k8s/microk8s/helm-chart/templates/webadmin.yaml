{{- if .Values.webAdmin.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "zotprime-k8s.fullname" . }}-zotprime-admin
  labels:
    app: {{ include "zotprime-k8s.fullname" . }}-zotprime-admin
  {{- include "zotprime-k8s.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.webAdmin.replicas }}
  selector:
    matchLabels:
      app: {{ include "zotprime-k8s.fullname" . }}-zotprime-admin
  template:
    metadata:
      labels:
        app: {{ include "zotprime-k8s.fullname" . }}-zotprime-admin
    spec:
      containers:
      - name: admin
        image: "{{ .Values.webAdmin.image.repository }}:{{ .Values.webAdmin.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.webAdmin.imagePullPolicy }}
        ports:
        - containerPort: {{ .Values.webAdmin.service.targetPort }}
        env:
        - name: WEBADMIN_USERNAME
          valueFrom:
            configMapKeyRef:
              name: {{ include "zotprime-k8s.fullname" . }}-webadmin-config
              key: webadmin-username
        - name: WEBADMIN_PASSWORD
          valueFrom:
            secretKeyRef:
              name: {{ include "zotprime-k8s.fullname" . }}-webadmin-secret
              key: webadmin-password
        - name: API_SUPER_TOKEN
          valueFrom:
            secretKeyRef:
              name: {{ include "zotprime-k8s.fullname" . }}-auth-secret
              key: api-super-token-hash
        resources:
          {{- toYaml .Values.webAdmin.resources | nindent 10 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "zotprime-k8s.fullname" . }}-admin
  labels:
    app: {{ include "zotprime-k8s.fullname" . }}-zotprime-admin
  {{- include "zotprime-k8s.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "zotprime-k8s.fullname" . }}-zotprime-admin
  ports:
  - port: {{ .Values.webAdmin.service.port }}
    targetPort: {{ .Values.webAdmin.service.targetPort }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "zotprime-k8s.fullname" . }}-admin-ingress
  labels:
    app: zotprime-admin
  {{- include "zotprime-k8s.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.tls.enabled }}
    cert-manager.io/cluster-issuer: letsencrypt-prod
    {{- end }}
spec:
  {{- if .Values.tls.enabled }}
  tls:
  - hosts:
    - {{ .Values.ingressHostnames.admin }}
    secretName: admin-tls
  {{- end }}
  rules:
  - host: {{ .Values.ingressHostnames.admin }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: {{ include "zotprime-k8s.fullname" . }}-admin
            port:
              number: {{ .Values.webAdmin.service.port }}
{{- end }}
