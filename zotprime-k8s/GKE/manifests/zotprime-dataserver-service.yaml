apiVersion: v1
kind: Service
metadata:
  labels:
    apps: dataserver
  name: dataserver
  namespace: zotprime
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  ports:
    - name: dataserver-api
      port: 80
      targetPort: 80
  selector:
    apps: zotprime-dataserver
  type: ClusterIP
status:
  loadBalancer: {}
