apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "zotprime-k8s.fullname" . }}-webadmin-config
  labels:
  {{- include "zotprime-k8s.labels" . | nindent 4 }}
data:
  webadmin-username: {{ .Values.webAdminConfig.username | quote }}
